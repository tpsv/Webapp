pipeline {
    agent none

    stages {
        stage("build") {
            agent any // Ensure a node is allocated for this stage
            steps {
                sh 'mvn clean package'
            }
        }
    }

    post {
        success {
            archiveArtifacts artifacts: '**/target/*.war'
        }
    }
}
